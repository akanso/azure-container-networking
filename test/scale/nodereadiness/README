(WIP)

For image: `make build-push`

Testing commands:
Run ./run-kwok.sh in the background.

`task setup`
`task viewresults` -> localhost:9002
`task scale -- 1000`

Cleanup:
`task scaledown`
`task teardown`

# For Podman login

sudo apt-get install podman -y

# GET ACR creds
az acr credential show --name acnpublic 

returns:
{
  "passwords": [
    {
      "name": "password",
      "value": "pass1"
    },
    {
      "name": "password2",
      "value": "pass2"
    }
  ],
  "username": "acnpublic"
}

# Login to ACR through podman
podman login acnpublic.azurecr.io -u acnpublic -p <pass>