kind: CiliumNetworkPolicy
apiVersion: cilium.io/v2
metadata:
  name: TEMP_NAME
  namespace: scale-test
spec:
  endpointSelector:
    matchLabels:
      app: nginx
      TEMP_LABEL_NAME: val
  egress:
    - toEndpoints:
      - {}